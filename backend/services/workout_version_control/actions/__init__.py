"""
Actions sub-package for 02_workout_version_control.

Contains:
    create   — manual & AI-stub plan creation
    update   — edit existing plans (auto-versions)
    delete   — soft-delete plans
    compare  — diff two plan versions
    rollback — revert to a previous snapshot
"""
